<UserControl
    x:Class="ClassicalMusicCSharp.Views.UserControls.StoreItemUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClassicalMusicCSharp.Views.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    xmlns:conv="using:ClassicalMusicCSharp.Converters">

    <UserControl.Resources>
        <conv:BoolToVisibility x:Key="BoolVisibility"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <SymbolIcon Height="35" Width="35" Margin="4,8" Symbol="{Binding Symbol}"/>
        <TextBlock Grid.Column="1" Margin="8" VerticalAlignment="Center" Text="{Binding Title}"/>
        <TextBlock Grid.Column="2" Margin="8" VerticalAlignment="Bottom" Text="{Binding Price}"/>
        <SymbolIcon Grid.Column="3" Symbol="Accept" Margin="4,8" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{Binding Buyed, Converter={StaticResource BoolVisibility}}"/>
    </Grid>
</UserControl>
