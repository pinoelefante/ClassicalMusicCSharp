<Page
    x:Class="ClassicalMusicCSharp.Views.RadioPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClassicalMusicCSharp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:vm="using:ClassicalMusicCSharp.ViewModels"
    xmlns:conv="using:ClassicalMusicCSharp.Converters"
    xmlns:controls="using:Template10.Controls">

    <Page.DataContext>
        <vm:RadioVM />
    </Page.DataContext>

    <Page.Resources>
        <conv:StringToUri x:Key="StringUri"/>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <controls:PageHeader BackButtonVisibility="Collapsed" Content="Live radio" IsEnabled="False" Frame="{x:Bind Frame}"/>

        <GridView Grid.Row="1" ItemsSource="{x:Bind VM.RadioList}" IsItemClickEnabled="True" ItemClick="{x:Bind VM.RadioSelected}">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Ellipse Grid.Row="0" Height="150">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="{Binding image, Converter={StaticResource StringUri}}"/>
                            </Ellipse.Fill>
                        </Ellipse>
                        <TextBlock Grid.Row="1" Text="{Binding title}"/>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
